[project]
name = "example-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "analysis",
    "mongo-worksim",
]

[tool.uv.sources]
mongo-worksim = { path = "../mongo_worksim" }
analysis = { path = "../analysis" }
